# SDIF v0.1 draft

This file is kept as a stable versioned entry point for readers and older links.
The authoritative SDIF v0.1 draft has been moved to [`docs/spec.md`](spec.md).

Use [`docs/spec.md`](spec.md) for the grammar, AST model, canonicalization rules, validation model, JSON mapping, CLI notes, and examples.
