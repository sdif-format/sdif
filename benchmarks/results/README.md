# Benchmark Results

This directory is reserved for curated or published benchmark evidence.

The token-efficiency runner writes in-progress evidence under `benchmarks/tmp/token_efficiency/` and moves successful runs to `benchmarks/results/token_efficiency/`. Keep large exploratory runs out of source control unless they are intentionally promoted as release evidence.
