# Benchmark Library

This directory is reserved for reusable benchmark helpers shared by multiple tracks.

Keep executable entrypoints in `benchmarks/scripts/`. Move reusable code here only when a second benchmark track needs it; avoid splitting code prematurely.
